// Tailwind CSS v4 is a PostCSS plugin; no tailwind.config.js is needed (theme
// lives in globals.css via @theme).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
